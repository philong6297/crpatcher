{
  "$defs": {
    "PatchConfig": {
      "additionalProperties": false,
      "properties": {
        "ext": {
          "default": "patch",
          "pattern": "^\\w+$",
          "title": "Ext",
          "type": "string"
        },
        "encoding": {
          "default": "utf-8",
          "title": "Encoding",
          "type": "string"
        },
        "replacement_separator": {
          "default": "-",
          "pattern": "^[\\w-]+$",
          "title": "Replacement Separator",
          "type": "string"
        }
      },
      "title": "PatchConfig",
      "type": "object"
    },
    "PatchInfoConfig": {
      "additionalProperties": false,
      "properties": {
        "version": {
          "default": 1,
          "minimum": 1,
          "title": "Version",
          "type": "integer"
        },
        "encoding": {
          "default": "utf-8",
          "title": "Encoding",
          "type": "string"
        },
        "ext": {
          "default": "patchinfo",
          "pattern": "^\\w+$",
          "title": "Ext",
          "type": "string"
        }
      },
      "title": "PatchInfoConfig",
      "type": "object"
    },
    "RepositoryConfig": {
      "additionalProperties": false,
      "properties": {
        "repo_dir": {
          "format": "path",
          "title": "Repo Dir",
          "type": "string"
        },
        "patch_dir": {
          "format": "path",
          "title": "Patch Dir",
          "type": "string"
        }
      },
      "required": [
        "repo_dir",
        "patch_dir"
      ],
      "title": "RepositoryConfig",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "repositories": {
      "items": {
        "$ref": "#/$defs/RepositoryConfig"
      },
      "title": "Repositories",
      "type": "array",
      "default": []
    },
    "patch_info_config": {
      "$ref": "#/$defs/PatchInfoConfig",
      "default": {}
    },
    "patch_config": {
      "$ref": "#/$defs/PatchConfig",
      "default": {}
    }
  },
  "$schema": "https://json-schema.org/draft-07/schema",
  "title": "CRPatcherConfig",
  "type": "object"
}